# GUI・通信評価 第002回（2026-09-14）

## 受入方針
現状ロジック・周期性能はREADMEへの実測値・制限明記を条件としてユーザーが許容。性能改善よりGUI・停止・再接続・通信断・ログを優先。従来周期目標のFailは書き換えない。

## 実操作による確認
対象: .tools/Pico CAN.app、bundle ID jp.local.pico-spi-canfd、host.main、tests/fixtures/gui_config.py（300秒）。GUI画面とログを照合。ソース変更・Pico再配置は今回行っていない。

- セッション開始: TabとSpace操作後に設定転送・ログ準備、受信中へ遷移。クリック単独の開始は確認できなかった。
- 受信表示: STD 100/200、FD、64バイト、00/FFデータ、受信回数の増加を画面で確認。
- aキー: T1が周期送信中となり、TX完了数が増加。表示データ1112131415161718。
- bキー: T1が停止済みとなり、次の画面確認までTX完了数701で停止。受信件数は増加。
- T2開始ボタンクリック: 複数回試みたが開始表示・件数増加は確認できず。UI操作ツールかアプリか原因未特定。Passにしない。
- 4 ID開始キー: 時間満了付近の操作となり、全ID稼働は確認できなかった。SESSION_NOT_RUNNINGの最新エラー表示を確認。
- 時間満了: DURATIONで停止済み表示となり、ログ排出と停止確認がtrue。ただし300秒設定に対し約35.757秒で終了。期待時間と不一致なのでFail（OPEN-020）。
- 終了: 停止状態からCommand+QでApp quitを確認。送信中のウィンドウ閉じる動作の代用にはしない。
- 再起動: bundle IDと実パスの両方で取得を試行したがtimeout。再接続の実操作は未確認。

## ログ照合
証跡: logs/gui/20260914-132543-227807（tx.txt/rx.txt/error.txt）、gui-analysis-002.json。
保存TX_DONE=705、RX=428。機器最終統計TX_DONE=705、RX=428。dropped=0、stop_confirmed=True、drain_complete=True。生ログを保持し、既存証跡は上書きしない。

## 自動試験（実操作とは別）
.venv314/bin/pytest -q: 79 passed、2 skipped。今回再実施。
実CAN2ケースは--can-port /dev/cu.usbmodem2303で試行したが、ポートopenでOperation not permittedとなり2 setup errors。アプリの合否ではなく環境ブロック。追加権限は読取のみが返り、書込は許可されていないため再試行していない。

## 残る実操作
- 開始／停止ボタン: クリック後に該当IDのTX開始／停止をログ照合。
- 同一ID置換: aでT1、iでT1Bを開始し、旧データ終了と新データ3132333435363738を確認。
- 全停止: 複数ID稼働からEscと全停止ボタンを別々に検証。停止確認と排出完了を照合。
- 送信枠超過: T1〜T4稼働中kでLIMITを要求し、拒否・最新エラー・既存送信継続を確認。
- 画面終了: 稼働中の閉じるボタンからCAN停止・ファイル排出・プロセス終了を確認。
- 再接続・物理USB断: 旧セッション混入なし、意図しない自動送信なしを確認。
- 時間満了: OPEN-020解消後、指定秒数の実経過と停止を照合。

以上は未実施・未確認であり合格扱いしない。電気測定は引き続き保留。将来のPWM統合は今回の対象外。全体受入完了は宣言しない。
